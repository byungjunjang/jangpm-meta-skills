interface:
  display_name: "Reflect"
  short_description: "Wrap up a session or capture a confirmed fix"
  default_prompt: "Use $reflect to wrap up this work session, or capture the concrete fix that was just confirmed."
policy:
  allow_implicit_invocation: true
